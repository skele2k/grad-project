This script is to process .gz files from ssdtrace-00.gz to ssdtrace-26.gz at once
Just excuting this script in the same directory with the ssdtrace-xx.gz files will work
Extracting only feautres with 
[Time stamp(in nanoseconds)] [Sector Number] [#of blocks(512byte per)]
